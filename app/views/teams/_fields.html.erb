<%= render 'shared/error_messages', :object => f.object %>
<p>A team must have a team name. Hackers add themselves to a team by viewing the teams and clicking the Join button.</p>
<%= f.input :name, :input_html => {:class => "large"} %>
<%= f.input :description %>
<%= f.button :submit %>